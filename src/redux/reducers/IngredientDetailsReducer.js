const initialState = {

}

export const ingredientDetailsReducer = (state = initialState, action) => {
    switch (action.type) {
        case 'SET_INGREDIENT_DETILS':
            return {

            }

        case 'RESET_INGREDIENT_DETILS':
            return {

            }

        default:
            return state
    }
}